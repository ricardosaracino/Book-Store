import {Injectable} from '@angular/core';

@Injectable()
export class BooksEffect {

  // Create an Effect that listens to an Action.
  // This Effect will call the BooksService, and will dispatch a Success Action
  // to populate Books State
}
